<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.torfin.mybulletjournal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splashscreen.SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.LoginActivity"/>

        <activity android:name=".taskslist.TasksListActivity"/>

        <activity android:name=".viewlabels.ViewLabelsActivity"
            android:label="@string/title_activity_view_labels"
            android:parentActivityName=".taskslist.TasksListActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".taskslist.TasksListActivity" />
        </activity>

        <activity android:name=".monthlylog.MonthlyLogActivity"
            android:label="@string/title_activity_monthly_log"
            android:parentActivityName=".taskslist.TasksListActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".taskslist.TasksListActivity" />
        </activity>

        <activity android:name=".futurelog.FutureLogActivity"
            android:label="@string/title_activity_future_log"
            android:parentActivityName=".taskslist.TasksListActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".taskslist.TasksListActivity" />
        </activity>

        <activity android:name=".newtask.AddNewTaskActivity"
            android:label="@string/title_activity_add_task"
            android:parentActivityName=".taskslist.TasksListActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".taskslist.TasksListActivity" />
        </activity>

        <activity android:name=".taskdetails.TaskDetailsActivity"
            android:parentActivityName=".taskslist.TasksListActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".taskslist.TasksListActivity" />
        </activity>

    </application>

</manifest>